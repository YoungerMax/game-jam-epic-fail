import { Path, PathPoint } from "./Path";

export const maxHealth = 200;

export const shipPath = new Path([
    new PathPoint(0, 2),
    new PathPoint(2, 6)
    // new PathPoint(1, 6),
    // new PathPoint(1, 0),
    // new PathPoint(3, 0),
    // new PathPoint(3, 3),
    // new PathPoint(5, 0),
    // new PathPoint(9, 4),
]);